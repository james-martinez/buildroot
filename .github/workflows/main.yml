name: I.MX6 RootFS Build

on: push


jobs:
  my-job:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: Build rootfs
      uses: docker://dvjhwf8qp/lanbuntu:latest
    - name: ggh
      run: echo "test test"
