name: I.MX6 RootFS Build

on: push

jobs:
  my-job:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - uses: docker://dvjhwf8qp/lanbuntu:latest
      with:
        ref: 2014.05
      env:
        br_config: wandboard_lancer_defconfig
    - name: Build
      run: | 
        make "$br_config"
        make
